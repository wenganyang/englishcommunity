# 英语交流社区 - EdgeOne 部署指南

## 📦 部署包信息
- **文件名**: `edgeone-deploy.zip`
- **包含文件**: index.html, styles.css, script.js, edgeone.json
- **域名**: www.applelele.top

---

## 🚀 部署步骤

### 步骤1：登录腾讯云 EdgeOne 控制台
1. 访问 https://console.cloud.tencent.com/edgeone
2. 使用腾讯云账号登录

### 步骤2：创建 Pages 项目
1. 在 EdgeOne 控制台，点击 **"Pages"** 菜单
2. 点击 **"创建项目"** 按钮
3. 选择 **"静态网站"** 类型

### 步骤3：上传项目文件
1. 在项目创建页面，选择 **"上传文件"** 方式
2. 点击 **"选择文件"** 按钮
3. 选择 `edgeone-deploy.zip` 文件
4. 等待上传完成

### 步骤4：配置项目
- **项目名称**: `english-community`（或自定义）
- **构建命令**: 留空（静态网站不需要构建）
- **输出目录**: 留空（使用根目录）
- **根目录**: 留空

### 步骤5：部署项目
1. 点击 **"部署"** 按钮
2. 等待部署完成（通常需要 1-2 分钟）
3. 部署成功后，会获得一个临时域名，例如：
   `https://english-community-xxx.edgeone.app`

---

## 🌐 配置自定义域名 (www.applelele.top)

### 步骤1：在 EdgeOne 添加域名
1. 进入项目设置
2. 点击 **"自定义域名"**
3. 点击 **"添加域名"**
4. 输入域名：`www.applelele.top`
5. 点击 **"验证"**

### 步骤2：获取 CNAME 记录值
EdgeOne 会提供一个 CNAME 记录值，例如：
```
xxx.edgeone.app
```

### 步骤3：在 DNSPod 添加解析记录
1. 访问 https://www.dnspod.cn 并登录
2. 进入域名 `applelele.top` 的解析管理
3. 点击 **"添加记录"**
4. 填写以下信息：
   - **主机记录**: `www`
   - **记录类型**: `CNAME`
   - **线路类型**: 默认
   - **记录值**: `xxx.edgeone.app`（EdgeOne 提供的值）
   - **TTL**: 600
5. 点击 **"保存"**

### 步骤4：验证域名归属权
1. 返回 EdgeOne 控制台
2. 按照提示添加 TXT 记录验证域名归属权
3. 在 DNSPod 添加对应的 TXT 记录
4. 等待验证完成（通常需要几分钟）

### 步骤5：配置 HTTPS（推荐）
1. 在 EdgeOne 控制台，为 `www.applelele.top` 开启 HTTPS
2. 选择 **"自动签发证书"**（免费）
3. 等待证书签发完成

---

## ✅ 验证部署

### 方法1：使用浏览器访问
```
https://www.applelele.top
```

### 方法2：使用命令行验证
```bash
nslookup www.applelele.top
```

应该显示 EdgeOne 的 CDN 节点 IP。

---

## 📋 故障排除

### 问题1：域名解析不生效
- **解决方案**: 等待 5-10 分钟，DNS 传播需要时间
- 使用 `nslookup www.applelele.top` 检查解析状态

### 问题2：访问显示 404
- **解决方案**: 检查文件是否正确上传到根目录
- 确认 `index.html` 文件存在

### 问题3：HTTPS 证书错误
- **解决方案**: 等待证书签发完成（可能需要几分钟）
- 检查域名解析是否正确

---

## 📞 技术支持
- 腾讯云文档: https://cloud.tencent.com/document/product/1552
- EdgeOne Pages 文档: https://edgeone.ai/pages

---

## 📝 项目功能
- ✅ 会员讨论区
- ✅ 词汇量检测
- ✅ 作文修改
- ✅ 用户登录/注册
- ✅ 个人中心

部署完成后，您的英语交流社区就可以通过 `https://www.applelele.top` 访问了！
